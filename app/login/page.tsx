'use client';import {FormEvent,useState} from 'react';import {signIn} from 'next-auth/react';import {useSearchParams,useRouter} from 'next/navigation';
export default function Login(){const p=useSearchParams(),r=useRouter();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();const res=await signIn('credentials',{email,password,redirect:false});if(res?.ok)r.push(p.get('callbackUrl')||'/dashboard');else setError('Invalid credentials. Check your email and password.');};return <main className="container-x flex min-h-[80vh] items-center justify-center pt-20"><form onSubmit={submit} className="w-full max-w-md rounded-[32px] border bg-white p-7 shadow-soft"><div className="text-sm font-black text-orange">WELCOME BACK</div><h1 className="mt-2 text-3xl font-black">Sign in to TravelGo</h1><p className="mt-2 text-sm text-slate-500">Use the configured TravelGo account to continue your booking.</p><div className="mt-6 grid gap-4"><label className="text-sm font-bold">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-2 w-full rounded-xl border px-4 py-3"/></label><label className="text-sm font-bold">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-2 w-full rounded-xl border px-4 py-3"/></label></div>{error&&<div className="mt-4 rounded-xl bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</div>}<button className="mt-6 w-full rounded-xl bg-orange py-3.5 font-black text-white">Sign In</button><div className="mt-5 rounded-xl bg-sky p-3 text-xs text-slate-600">Use the account configured for this TravelGo installation.</div></form></main>}
