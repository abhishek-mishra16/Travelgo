'use client';import {useEffect,useState} from 'react';export default function Offers(){const[o,setO]=useState<any[]>([]);useEffect(()=>{fetch('/api/offers').then(x=>x.json()).then(j=>setO(j.offers||[]))},[]);return <main className="container-x pt-24 pb-20"><span className="text-xs font-black uppercase tracking-[.2em] text-orange">Travel offers</span><h1 className="mt-2 text-4xl font-black">Travel more, save smarter.</h1><p className="mt-2 text-slate-500">Apply these offers during checkout.</p><div className="mt-8 grid gap-5 md:grid-cols-3">{o.map(x=><div key={x.code} className="rounded-3xl border bg-white p-6 shadow-sm"><div className="text-4xl font-black text-orange">{x.value}</div><h2 className="mt-3 text-xl font-black">{x.title}</h2><div className="mt-5 flex items-center justify-between rounded-xl bg-sky p-3"><code className="font-black">{x.code}</code><button onClick={()=>navigator.clipboard?.writeText(x.code)} className="text-xs font-black text-navy">Copy</button></div></div>)}</div></main>}
