import {NextResponse} from 'next/server';import {getServerSession} from 'next-auth';import {authOptions} from '@/lib/auth';export async function GET(){const s=await getServerSession(authOptions);if((s?.user as any)?.role!=='ADMIN')return NextResponse.json({error:'Forbidden'},{status:403});return NextResponse.json({users:12840,bookings:3284,revenue:2480000,activeTrips:126,cancellationRate:4.8});}
