'use client';
import Link from 'next/link';import {Bus,TrainFront,Plane,Clock3,Star,Wifi,ChevronRight} from 'lucide-react';import {Trip} from '@/lib/types';
const icons={BUS:Bus,TRAIN:TrainFront,FLIGHT:Plane};function time(s:string){return new Date(s).toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit',hour12:false})}function duration(m:number){return `${Math.floor(m/60)}h ${m%60}m`}
export function TripCard({trip,badge}:{trip:Trip;badge?:string}){const Icon=icons[trip.type];return <article className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-soft"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-sky text-navy"><Icon/></span><div><div className="font-black">{trip.operator}</div><div className="text-xs text-slate-500">{trip.service || trip.type} · {trip.rating.toFixed(1)} ★</div></div></div>{badge&&<span className="rounded-full bg-green/10 px-3 py-1 text-xs font-black text-green">{badge}</span>}</div><div className="mt-6 grid grid-cols-[1fr_auto_1fr_auto] items-center gap-3"><div><div className="text-xl font-black">{time(trip.departure)}</div><div className="text-xs text-slate-500">{trip.originCode}</div></div><div className="text-center text-xs text-slate-400"><div className="h-px w-20 bg-slate-200"/><div className="mt-1">{duration(trip.durationMinutes)}</div></div><div><div className="text-xl font-black">{time(trip.arrival)}</div><div className="text-xs text-slate-500">{trip.destinationCode}</div></div><div className="text-right"><div className="text-xl font-black text-orange">₹{trip.price.toLocaleString('en-IN')}</div><div className="text-xs text-slate-500">{trip.stops===0?'Non-stop':`${trip.stops} stop`}</div></div></div><div className="mt-5 flex flex-wrap gap-2">{trip.amenities.slice(0,4).map(a=><span key={a} className="rounded-full bg-slate-50 px-3 py-1 text-xs font-semibold text-slate-600">{a}</span>)}</div>{trip.source && <div className="mt-3 text-[11px] text-slate-400">Fare source: {trip.source}{trip.sourceUrl ? <a className="ml-1 font-bold text-navy hover:text-orange" href={trip.sourceUrl} target="_blank" rel="noreferrer">View source</a> : null}</div>}<div className="mt-5 flex gap-2"><Link href={`/trips/${trip.id}?from=${trip.originCode}&to=${trip.destinationCode}`} className="flex-1 rounded-xl border border-navy/15 px-4 py-3 text-center text-sm font-black text-navy hover:bg-sky">View Details</Link><Link href={`/booking/seat-selection/${trip.id}?from=${trip.originCode}&to=${trip.destinationCode}`} className="flex-1 rounded-xl bg-orange px-4 py-3 text-center text-sm font-black text-white">Select <ChevronRight className="inline h-4 w-4"/></Link></div></article>}
